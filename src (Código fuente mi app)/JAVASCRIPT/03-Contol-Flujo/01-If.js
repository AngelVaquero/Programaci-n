// if (Condicion)
    // Expresion

// if (Condicion) {
    // Varias
    // Expresiones
    // O Varias líneas
// }
// Si se cumple la condición, podemos añadir la expresión
let Edad = 25;

if (Edad > 17) {
    console.log('Usuario mayor de edad');
}

let OtraEdad = 15;

if (OtraEdad > 17) {
    console.log('Usuario mayor de edad');
}